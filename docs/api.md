# API
Public REST endpoints are documented in the root README. Mutating routes require a bearer token. `X-Request-ID` is generated for every HTTP request. Health and readiness are unauthenticated so orchestration can probe the process.
