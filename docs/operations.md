# Operations
Heartbeat liveness target is 15 seconds. Inspect `/health`, `/ready`, `/metrics`, NATS consumer state, PostgreSQL connection pressure and Redis heartbeat keys during incidents. Treat DLQ growth as a signal requiring workload/application inspection.
